import styled from 'styled-components';

export const Wrapper = styled('div')`
    min-height: 100vh;
    padding: 30px;
    background: #d3f2fa;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 32px;
}
`;