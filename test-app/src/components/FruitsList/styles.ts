import styled from 'styled-components';

export const Wrapper = styled('div')`
    display: grid;
    gap: 20px;
    padding: 20px;
    
    select {
        padding: 16px;
        outline: none;
    }
`;
